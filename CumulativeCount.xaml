<Window x:Class="KIT206_GUI.CumulativeCount"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KIT206_GUI"
        mc:Ignorable="d"
        Title="CumulativeCount"  Height="400" Width="450" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Icon="Group-icon.png">
    <Grid x:Name="CumulativeCount1">
        <DataGrid Name="CumulativeCountDGrid" Margin="0" IsReadOnly="True" AutoGenerateColumns="False" HeadersVisibility="Column">
            <DataGrid.Columns >
                <DataGridTextColumn Header="Year" Binding="{Binding Path=year}" />
                <DataGridTextColumn Header="Publications" Binding="{Binding Path=numPublications}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
